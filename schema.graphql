schema {
	query: Query
	# mutation: Mutation
}

# The query type, represents all of the entry points into our object graph
type Query {
	AllTransactions(address: String = "", hash: String = "", id: String = ""): String
}
